# MoviePilot 自建插件上下文

本上下文记录用户自建 MoviePilot 插件库里的领域语言。它只定义概念，不记录实现方案、配置值或密钥。

## Language

**站点条目**:
PT 站点上的一个资源详情记录，包含站点资源 ID、标题、副标题、标签、文件列表、大小、做种信息、封面和简介等可解析信息。
_Avoid_: 种子、资源、页面

**种子元信息**:
`.torrent` 文件或下载器任务中记录的元数据，通常包含任务名、文件路径列表、文件大小、分片哈希和 tracker 信息。它可以确认下载内容包里是否声明了 NFO、海报等旁挂文件，但不等同于这些文件的实际内容。
_Avoid_: 种子内容、媒体资料、NFO 内容

**下载内容包**:
下载完成后落在下载目录中的真实目录树和文件集合，包括媒体文件、字幕、NFO、poster、fanart、sample、说明文件等。插件做本地识别时应优先读取下载内容包，而不是只依赖站点标题或种子元信息。
_Avoid_: 下载任务、种子、文件夹

**随包元数据**:
下载内容包中随媒体一起发布的旁挂元数据和图片，例如 `movie.nfo`、`tvshow.nfo`、`*.nfo`、`poster.jpg`、`fanart.jpg`、字幕文件和说明文件。它是儿童动画离线识别的优先事实来源。
_Avoid_: 刮削结果、站点简介、搜索结果

**本地媒体条目**:
当 TMDb 或豆瓣缺少对应数据时，由插件维护并可作为 MoviePilot 插件媒体来源返回的媒体元数据记录，用于代表一部电影、一个剧集季或一个儿童节目合集。
_Avoid_: 伪 TMDb、假媒体、手工信息

**识别兜底**:
MoviePilot 默认媒体识别失败后，由自建插件提供标题、年份、季集、类型、简介和图片等信息的补救路径。
_Avoid_: 强制识别、硬改识别、绕过识别

**下载后识别**:
插件在下载加入、下载完成或整理失败后，根据下载内容包、随包元数据、种子元信息和站点条目重新生成媒体身份，再把结果交给 MoviePilot 继续整理。
_Avoid_: 人工整理、重新下载、全网搜索

**插件媒体来源**:
由插件注册到 MoviePilot 的媒体数据源，拥有稳定的 `media_source` 标识和来源内 `media_id`，可参与搜索、识别、详情和整理链路。
_Avoid_: 本地库、假来源、私有 TMDb

**整理入库**:
把下载完成的文件按媒体库目录规范放置，并提供媒体服务器可读取的本地元数据和图片，使 Emby/Jellyfin/Plex 可以建立库记录。
_Avoid_: 下载、转移、刮削

**解析规则**:
从站点标题、副标题、简介和文件列表中提取片名、别名、年份、季号、集数、分辨率、来源、音轨、制作组等字段的一组规则。
_Avoid_: 正则、匹配逻辑

**自动补全**:
插件在默认识别失败或元数据不完整时，按来源优先级自动收集、合并和规范化媒体信息，形成可供 MoviePilot 继续处理的媒体身份。
_Avoid_: 人工校正、手工补资料、全自动乱搜

**贡献草稿**:
插件把已整理的本地媒体条目转换成可提交到 TMDb 的字段清单、图片清单和来源证据，但不自动提交到 TMDb。
_Avoid_: 自动编辑 TMDb、机器提交、强制同步
